fn main() {
    let xs = list_create()
    list_push(xs, 1)
    list_push(xs, 2)
    list_push(xs, 3)

    xs[1] = 20
    xs[2] += 7

    let a = xs[0]
    let b = xs[1]
    let c = xs[2]
    print("a={a} b={b} c={c}\n")
}
