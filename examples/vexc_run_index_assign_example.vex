use "../src/vexc/eval.vex"

fn main() {
    print("[vexc] running examples/index_assign.vex\n")
    let token_cache = vexc_token_cache_create()
    let prog = vexc_parse_program_file_cached("examples/index_assign.vex", token_cache)
    let v = vexc_eval_program(prog)
    print("\n[vexc] returned {v}\n")
}
